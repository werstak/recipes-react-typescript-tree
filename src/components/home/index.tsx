import * as React from 'react';
import TodoList from '../TodoList/TodoList';

const Greating: React.FunctionComponent = () => {
    return (
        <div>
            <TodoList />
        </div>
    )
};

export default Greating;
